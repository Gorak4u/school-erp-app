[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x", "python3", "gcc", "glibc"]

[phases.install]
cmds = [
  "npm ci --include=dev --legacy-peer-deps"
]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build"
]

[start]
cmd = "npm start"
